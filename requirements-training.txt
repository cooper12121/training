# Install in the selected Merlin/Arnold CUDA image with its matching PyTorch.
# DeepSpeed is optional; install an image-compatible build when enabling it.
transformers==4.51.3
accelerate==1.6.0
datasets==3.5.1
peft==0.15.2
pandas==2.2.3
tqdm>=4.67,<5
